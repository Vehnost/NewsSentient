# Core dependencies - using newer versions with Python 3.13 support
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0

# Async and HTTP - httpx only (no aiohttp to avoid compilation)
httpx>=0.27.0

# RSS and News parsing
feedparser>=6.0.11
beautifulsoup4>=4.12.3

# Utilities
python-dotenv>=1.0.0
requests>=2.32.0
python-dateutil>=2.9.0

# Logging
loguru>=0.7.2

# Testing (optional)
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Note: Removed aiohttp to avoid C++ compilation requirements
# Note: Using newer versions that have pre-built wheels for Python 3.13
# The agent works fully with httpx for async HTTP requests
